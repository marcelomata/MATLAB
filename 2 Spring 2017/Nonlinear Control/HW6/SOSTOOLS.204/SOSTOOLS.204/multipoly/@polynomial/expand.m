function b = expand(a)
% function B = expand(A)
%
% DESCRIPTION 
%   Expand a polynomial into a sum of monomials.  Since polynomial
%   objects are stored in expanded form, this function currently
%   just returns the input.  
%   
% INPUTS 
%   A: polynomial 
%
% OUTPUTS  
%   B: polynomial
%  
% SYNTAX 
%   B = expand(A);   
%
  
% 4/3/2003: PJS  Initial Coding
  
% Currently just pass back the input
b=a;
